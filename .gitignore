# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local

# Raw game asset packs (composed outputs live in public/game)
Ninja Adventure - Asset Pack/
Sprout Lands - Sprites - Basic pack/

# LPC avatar download cache
.lpc-cache/

# Sunnyside asset pack, re-extracted by scripts/convert-sunnyside-room.mjs (47 MB)
tiled/extracted/

# Generated doodle sheets kept by scripts/backfill-doodles.mjs for inspection
doodle-sheets/

# Local Supabase CLI state — project link cache, branch info, and per-run
# secrets for `supabase functions serve` (start-secrets/.../docker.env holds
# real API keys, the DB URL, and the service role key — never commit this).
supabase/.temp/
supabase/.branches/
